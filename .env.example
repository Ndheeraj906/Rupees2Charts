# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ruppes2bar
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here

# Express session secret — generate with: node -e "require('crypto').randomBytes(48,(_,b)=>console.log(b.toString('hex')))"
SESSION_SECRET=replace_with_a_random_secret

# Server port
PORT=3000
